---
import type { HTMLTag } from "astro/types";
import * as interfaces from "@/lib/interfaces.ts";
import RichText from "@/components/notion-blocks/RichText.astro";

export interface Props {
	richTexts: interfaces.RichText[];
	block?: interfaces.Block;
	as?: HTMLTag;
	footnotes?: interfaces.Footnote[];
}

const { richTexts, block, as: Tag = "div", footnotes } = Astro.props;
---

{
	(
		<Tag class="caption text-textColor/70 min-w-0 pt-1 text-sm">
			{richTexts.map((richText: interfaces.RichText) => (
				<RichText richText={richText} blockID={block.Id} footnotes={footnotes} />
			))}
		</Tag>
	)
}
